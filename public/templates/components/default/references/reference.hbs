const {{replaceId resourceConfig.name resourceConfig.component}} = useRef{{#if resourceConfig.type}}<{{{replaceType resourceConfig.type resourceConfig.component}}}>{{/if}}({{{replaceValue (resolveStateDefault
  resourceConfig.defaultValue (replaceType resourceConfig.type resourceConfig.component)) resourceConfig.component}}});