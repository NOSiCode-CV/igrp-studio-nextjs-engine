const [{{resourceConfig.name}}, set{{capitalize resourceConfig.name}}] = useState{{#if resourceConfig.type}}<{{{resourceConfig.type}}}>{{/if}}({{{resolveStateDefault
  resourceConfig.defaultValue resourceConfig.type}}});