const [{{resourceConfig.name}}, set{{capitalize resourceConfig.name}}] = useState{{#if resourceConfig.type}}<{{{resourceConfig.type}}}{{#if resourceConfig.isList}}[]{{/if}}>{{/if}}({{{resolveStateDefault
  resourceConfig.defaultValue resourceConfig.type}}});