<IGRPTable{{{typeResolution (resolveFirstType resourceConfig.properties.data)}}}
  content={ contentTable{{resourceConfig.id}} }
  columns={
    [
    {{#if resourceConfig.properties.columns}}
      {{#each resourceConfig.properties.columns}}
      {
        header: '{{#if this.properties.labelText}}{{this.properties.labelText}}{{else}}{{capitalize this.id}}{{/if}}',
        accessorKey: '{{toCamelCaseFromNatural this.id}}' as keyof {{resolveFirstType ../resourceConfig.properties.data}},
        render: (value: string) => {{{render-table-row this ../registry}}}
      },
      {{/each}}
    {{/if~}}
    ]
  }
  {{#if resourceConfig.properties.actions}}
  actions={({ original }) => (
    <div className="flex justify-end gap-2">
      {{#each resourceConfig.properties.actions}}
      {{{render-layout this}}}
      {{/each}}
    </div>
  )}
  {{/if}}
/>