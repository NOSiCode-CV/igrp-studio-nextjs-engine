const rowData = row.original;

const { iconName, bgClass, textClass, label, className } = {{resourceConfig.interactions.customize.function.fnName}}(rowData);

return <IGRPDataTableCellBadge
  {{!--field={ row.getValue('{{resourceConfig.tag}}') }--}}
  label={ {{#if resourceConfig.data.value.code}}{{{resourceConfig.data.value.code}}}{{else}}label ?? row.original.{{resourceConfig.tag}}{{/if}} }
  {{#if resourceConfig.properties.variant}}
  variant="{{resourceConfig.properties.variant}}"
  {{/if~}}
  className={ `${bgClass} ${textClass} ${className}` }
>

</IGRPDataTableCellBadge>