<IGRPInfoCard
  {{#if resourceConfig.properties.commonProperties.generateReference}}
  ref={ getSectionRef('{{resourceConfig.tag}}') }
  data-section-id={ '{{resourceConfig.tag}}' }
  {{/if}}
  {{#if (and resourceConfig.properties.variantSection (not resourceConfig.data.variantSection))}}
  variantSection={ "{{resourceConfig.properties.variantSection}}" }
  {{/if~}}
  {{#if (and resourceConfig.properties.colorSection (not resourceConfig.data.colorSection))}}
  colorSection={ "{{resourceConfig.properties.colorSection}}" }
  {{/if~}}
  {{#if (and resourceConfig.properties.titleClassName (not resourceConfig.data.titleClassName))}}
  titleClassName={ "{{resourceConfig.properties.titleClassName}}" }
  {{/if~}}
  {{#if (and resourceConfig.properties.title (not resourceConfig.data.title))}}
  title={ "{{resourceConfig.properties.title}}" }
  {{/if~}}
  {{#if (and resourceConfig.properties.className parentResourceConfig)}}
  className={ cn({{{concat "'" resourceConfig.properties.className "'," (addClassNameFromChildProperties parentResourceConfig registry) (addClassNameFromProperties resourceConfig registry) (addClassNameFromStyle resourceConfig.style)}}}) }
  {{else}}
  {{#if (and resourceConfig.properties.className (not parentResourceConfig))}}
  className={ cn({{{concat "'" resourceConfig.properties.className "'," (addClassNameFromProperties resourceConfig registry) (addClassNameFromStyle resourceConfig.style)}}}) }
  {{else}}
  {{#if (and (nullOrEmpty resourceConfig.properties.className) parentResourceConfig.childProperties)}}
  className={ cn({{{concat (addClassNameFromChildProperties parentResourceConfig registry) (addClassNameFromProperties resourceConfig registry) (addClassNameFromStyle resourceConfig.style)}}}) }
  {{/if~}}
  {{/if}}
  {{/if}}
  {{#if resourceConfig.data}}
  {{{render-data resourceConfig.data resourceConfig}}}
  {{/if}}
  sections={
    [
    {{#if resourceConfig.children}}
      {{#each (extractInfoSection resourceConfig.children)}}
      {{#if this.data.items}}
      {
        items: {{this.data.items.state.name}}
      },
      {{else}}
      {
        items: [
        {{#each (extractInfoItem this.children)}}
          {
          {{#if this.data.text}}
            text: {{this.data.text.state.name}},
          {{else}}
            {{#if this.properties.text}}
              text: "{{this.properties.text}}",
            {{else}}
              text: "",
            {{/if~}}
          {{/if~}}
          {{#if this.data.label}}
            label: {{this.data.label.state.name}},
          {{else}}
            {{#if this.properties.label}}
              label: "{{this.properties.label}}",
            {{else}}
              label: "",
            {{/if~}}
          {{/if~}}
          {{#if this.data.icon}}
            icon: {{this.data.icon.state.name}},
          {{else}}
            {{#if this.properties.iconProperties.icon}}
              icon: "{{this.properties.iconProperties.icon}}",
            {{/if~}}
          {{/if~}}
          {{#if this.data.showIcon}}
            showIcon: {{this.data.showIcon.state.name}},
          {{else}}
            {{#if this.properties.iconProperties.showIcon}}
              showIcon: {{this.properties.iconProperties.showIcon}},
            {{/if~}}
          {{/if~}}
          {{#if this.data.iconClassName}}
            icon: {{this.data.iconClassName.state.name}},
          {{else}}
            {{#if this.properties.iconProperties.iconClassName}}
              iconClassName: "{{this.properties.iconProperties.iconClassName}}",
            {{/if~}}
          {{/if~}}
          {{#if this.data.variantItem}}
            variantItem: {{this.data.variantItem.state.name}},
          {{else}}
            {{#if this.properties.variantItem}}
              variantItem: "{{this.properties.variantItem}}",
            {{/if~}}
          {{/if~}}
          {{#if this.data.colorItem}}
            colorItem: {{this.data.colorItem.state.name}},
          {{else}}
            {{#if this.properties.colorItem}}
              colorItem: "{{this.properties.colorItem}}",
            {{else}}
              colorItem: "",
            {{/if~}}
          {{/if~}}
          },
        {{/each}}
        ]
      },
      {{/if~}}
      {{/each}}
    {{/if~}}
    ]
  }
/>