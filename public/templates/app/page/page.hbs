'use client'

import { useState } from "react";

{{{resolve-imports resourceConfig.components}}}

export default function PageComponent() {
{{#if (notNullOrEmpty resourceConfig.components)}}
  {{{resolve-states resourceConfig.components}}}

  return (
{{{render-layout resourceConfig.components}}}
  );
{{/if}}
}