'use client'

{{{resolve-imports resourceConfig.types resourceConfig.components registry resourceConfig.pageName basePath}}}

export default function Page{{capitalize (toCamelCaseFromNatural resourceConfig.pageName)}}Component() {
{{#if (notNullOrEmpty resourceConfig.components)}}

  const service: Partial<I{{toLowerCase resourceConfig.pageName}}Service> = {{capitalize resourceConfig.pageName}}Service;

  {{{resolve-states resourceConfig.components registry}}}
  {{{resolve-code-blocks resourceConfig.components registry}}}

  return (
{{{render-layout resourceConfig.components}}}
  );
{{/if}}
}

export type I{{toLowerCase resourceConfig.pageName}}Service = {
  {{{resolve-service-interface-methods resourceConfig.components registry}}}
}