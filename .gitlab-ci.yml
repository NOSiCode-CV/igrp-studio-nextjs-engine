stages:
  - build
build_package_dev:
  image: node
  stage: build
  only:
    - master
  script:
    - npm install
    - npm run build
    - npm publish --registry=https://sonatype.nosi.cv/repository/igrp/
